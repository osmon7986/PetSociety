namespace PetSociety.API.Enums.Events
{
    public enum ActivityStatus
    {
        Pending = 0,   // 審核中
        Published = 1, // 已上架
        Rejected = 2   // 已退件
    }
    public enum Status
    {
        Pending = 0,//Todo先寫著
        Published = 1,
        Rejected = 2
    }
}
